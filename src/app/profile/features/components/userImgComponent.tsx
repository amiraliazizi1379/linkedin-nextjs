export default function UserImageComponent({ style }: { style: string }) {
  return (
    <div
      className={`${style}  rounded-full bg-[#c0392b] flex-center text-[#fff]`}
    >
      A
    </div>
  );
}
